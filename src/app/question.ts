export class Question {
    public  questionId: number;
    public  questionText: string;


    // method to initialize properties
    // either remove the setter or use it to set values
    // else you will get compile error 
/*
    setQuestion(qID: number, qText: string) {
    this.questionId = qID;
    this.questionText = qText;
  }
  */

}  // End the class
